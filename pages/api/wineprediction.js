export default function handler(req, res) { 
    if(req.method === 'POST'){
        const wineInputs = req.body.wineInputs
        const newWineInput = {

        } 
        wineInputs.post().then
    }
}